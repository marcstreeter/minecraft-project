{{- if .Values.hub.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "minecraft-project.fullname" . }}-hub
  labels:
    {{- include "minecraft-project.labels" . | nindent 4 }}
    app.kubernetes.io/component: hub
spec:
  type: {{ .Values.hub.service.type }}
  ports:
    - protocol: TCP
      port: {{ .Values.hub.service.port }}
      targetPort: {{ .Values.hub.service.port }}
  selector:
    {{- include "minecraft-project.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: hub
{{- end }} 