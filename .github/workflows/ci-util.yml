name: build our image

on:
  push:
    paths:
      - util/**
    branches: master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: checkout code
        uses: actions/checkout@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: install buildx
        id: buildx
        uses: crazy-max/ghaction-docker-buildx@v1
        with:
          version: latest
      - name: build the utils image
        working-directory: ./util
        run: |
          docker buildx build \
            --push \
            --tag marcstreeter/utils:latest \
            --platform linux/amd64,linux/arm/v7,linux/arm64 .
      - name: Install Just
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://just.systems/install.sh | bash -s -- --to /usr/local/bin
      - name: Get latest version and update manifests
        run: |
          LATEST_VERSION=$(just get-latest-server-version marcstreeter/utils)
          echo "Latest version: $LATEST_VERSION"
          
          # Update all util K8s manifests (they use utils image in initContainers and sidecars)
          sed -i "s|image: marcstreeter/utils:.*|image: marcstreeter/utils:$LATEST_VERSION|g" kubernetes/deploy-world.yml
          sed -i "s|image: marcstreeter/utils:.*|image: marcstreeter/utils:$LATEST_VERSION|g" kubernetes/deploy-world-berry.yml
          sed -i "s|image: marcstreeter/utils:.*|image: marcstreeter/utils:$LATEST_VERSION|g" kubernetes/deploy-world-ice.yml
          sed -i "s|image: marcstreeter/utils:.*|image: marcstreeter/utils:$LATEST_VERSION|g" kubernetes/deploy-world-lily.yml
          sed -i "s|image: marcstreeter/utils:.*|image: marcstreeter/utils:$LATEST_VERSION|g" kubernetes/deploy-world-sand.yml
          sed -i "s|image: marcstreeter/utils:.*|image: marcstreeter/utils:$LATEST_VERSION|g" kubernetes/deploy-world-wood.yml
          sed -i "s|image: marcstreeter/utils:.*|image: marcstreeter/utils:$LATEST_VERSION|g" kubernetes/deploy-proxy.yml
          sed -i "s|image: marcstreeter/utils:.*|image: marcstreeter/utils:$LATEST_VERSION|g" kubernetes/deploy-hub.yml
          
          # Commit changes
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add kubernetes/deploy-*.yml
          git commit -m "Update utils image to $LATEST_VERSION"
          git push
